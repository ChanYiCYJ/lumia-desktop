name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [x64]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        # GitHub Actions 在美国：用官方 registry + GitHub Electron 下载（最快），无需国内镜像
        run: npm ci --no-audit --no-fund
      - name: Build & package (AppImage + deb)
        run: npm run build:linux
      - uses: actions/upload-artifact@v4
        with:
          name: lumia-desktop-linux
          path: |
            dist/*.AppImage
            dist/*.deb
          if-no-files-found: error

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Build & package (NSIS)
        run: npm run build:win
      - uses: actions/upload-artifact@v4
        with:
          name: lumia-desktop-windows
          path: |
            dist/*.exe
            dist/*.blockmap
          if-no-files-found: error

  release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**
          generate_release_notes: true
