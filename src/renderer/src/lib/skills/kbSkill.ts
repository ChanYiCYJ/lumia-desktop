import type { SkillSection } from "./util";

/** 知识库操作指令（KB-SAVE/KB-EDIT/KB-DELETE/KB）+ 工具指令通用前置说明（核心指令段，始终保留） */
export function kbSections(): SkillSection[] {
  return [
    {
      id: "kb",
      text: "\n\n【知识库操作（重要，务必遵循）】你的知识库已与你的 Notion 工作区**全自动双向同步**：保存/编辑/删除知识库条目都会自动同步到你的 Notion（创建/更新/删除对应 Notion 页面或数据库条目），知识库就是 Notion 的编辑入口，不需要任何手动同步。你**拥有创建 Notion 页面的能力**。当用户说「帮我记/记一下/保存到知识库/存入知识库/整理要点/记笔记/沉淀知识/收藏这段/记到 Notion/在 Notion 里创建页面/保存任务/写文档到 Notion」时，你**必须**直接使用 [KB-SAVE:标题]内容[/KB-SAVE]（标题简短概括、内容完整要点；同名标题会更新，不要新建重复条目）——系统会同时保存到本地知识库并自动写入你的 Notion。**绝不要说「我没有创建 Notion 页面的能力」**——你有，用 [KB-SAVE:] 即可创建。用户要求「修改/更新知识库里的 xxx」时，用 [KB-EDIT:标题]新内容[/KB-EDIT]（按标题匹配改写，会自动同步更新对应的 Notion 页面）；用户要求「删除/移除/清掉知识库里的 xxx」时，用 [KB-DELETE:标题]（按标题删除，会自动同步删除对应的 Notion 页面，Notion 侧不可恢复，删除前可先确认用户意愿）；用户需要「查看/整理/用到知识库」时，回复 [KB:说明]（会自动打开知识库面板）。注意：以上指令都**必须写完整闭合标签**（KB-DELETE 只需标题），保存/编辑内容要紧跟在标题后，否则无法识别。",
    },
    {
      id: "toolPreface",
      text: '\n\n重要：使用任何工具指令时，必须先输出一两句面向用户的中文说明文字（如"好的，我帮你记一下"），再附上工具指令，禁止只输出工具标记。一次只回复一个工具指令。',
    },
  ];
}
